<%= simple_form_for(@item) do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.input :long_description %>
  <%= f.select :power, Item::POWER, label: "Niveau de l'objet" %>
  <%= f.select :category, Item::CATEGORY, label: "Catégorie de l'objet" %>
  <%= f.input :image, as: :file, label: "Image de l'objet" %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to Items", items_path, class: "btn btn-secondary" %>
